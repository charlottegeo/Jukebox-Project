@critical
✔ fix playback issues on display page @done(24-10-21 04:17)
✔ fix issue on search page where you must refresh to see your updated queue (in the case where your song was loaded into display page) @done(24-10-24 08:34)
☐ add all necessary round robin queue-related functions
☐ test round robin with 1 or 2 other people
☐ It seems that the round robin system is broken?
    ☐ From what i can tell, it is rotating between users but playing all the user's songs before switching
    ☐ It should be playing the first song 
    ☐ Ok it is working now? But i want to make sure that the logic is sound
    ☐ Potentially it has something to do with users not having their pages open
✔ make sure queue updates visually when albums and playlists and songs added from links are added @done(24-11-08 07:17)
✔ When page refreshes, show the queue that was there before the refresh @done(24-11-08 07:26)
@high
✔ get username to add to queue (i think i do need socketio session for this) @done(24-10-25 19:12)
✔ make an isAdmin boolean to determine when a user logs in if they are an admin based on if they are in the rtp or eboard groups or if their username is ccyborgg @done(25-03-24 20:28)
✔ Only show admin panel if the user is an admin @done(25-03-24 20:28)
✔ when a song loads from the queue, load the song info but have a loading animation while waiting for the song to download + load @done(25-03-24 20:49)
☐ Add a 1m grace period if a user disconnects before they are removed from the round robin system (not removing their queue, just removing their username from the round robin rotation)
☐ If a user is removed from the round robin system, keep their queue in the system, but remove their username from the round robin rotation
✔ make sure when a user refreshes the page, their queue is still there @done(25-03-24 20:52)
☐ The next song in the queue cannot be removed or moved in the queue, it should be locked and indicated as such, and that song will download while the current song is playing
    ☐ if the song doesn't finish downloading before the current song ends, load the song info and show it loading until it is ready to play
✔ Add banner above search section but below navbar that shows the next song, the current song, the vote to skip button, and the number of users in the queue @done(25-03-25 00:03)
    ✔ Indicate if the current song is paused or playing @done(25-03-25 00:03)
✔ Replace progress bar with loading animation when a song is loading then show the bar when it is ready to play @done(25-03-25 00:26)
☐ Make sure bpm analysis and cat animation work for different beats and time signatures
☐ save bpm when song is paused so when it is played again, the cat animation continues from where it left off
@low
☐ admin commands
    ✔ force skip song @done(24-11-01 10:24)
    ✔ pause/play @done(25-03-24 17:58)
    ✔ view active users (a scrollable row of users in the queue, with their image (circular) and username and number of songs in their queue below their image) @done(25-03-25 15:50)
    ✔ refresh display (in case something breaks? might not need this if you ensure no bugs in queue playback) @done(24-11-01 10:43)
    ✔ adjust volume with SSH (or a simpler/more secure way?) @done(25-03-24 17:59)
    ✔ adjust song length limit (hard max length of 10 minutes, no minimum length) @done(25-03-25 15:51)
☐ make display page authenticated in some way, separated from the sso on the search page.
✔ ensure that display page login is not added to queue @done(25-03-24 21:15)
☐ put the proper functions as auth only
✔ add dropdown to search page (and remove the one from display page) to update user color (the color for a given user) @done(25-03-25 15:51)
✔ make cat animation update with new color when color is changed @done(25-03-25 17:21)
☐ when a song loads from the queue, update the cat animation to reflect the new user's color
☐ if a user whose song is currently playing changes their color, update the cat animation to reflect the new color
☐ Store user color in session so if they refresh the page or leave and come back, their color is still the same
✔ make admin panel a popup or dropdown (or even a sidebar), instead of a separate page? @done(24-11-08 18:11)
✔ make it so you need to click song twice to add to queue @done(25-03-24 21:10)
    ✔ highlight selected song when first clicked @done(25-03-24 21:10)
    ✔ if the user clicks a different song, highlight that one instead, removing previously selected @done(25-03-24 21:10)
    ✔ de-select if the user clicks on something other than the songs @done(25-03-24 21:10)
☐ highlight the first song in the next user's queue to indicate that their song is up next
    ☐ if the user re-orders their queue, indicate the new upcoming song
✔ add message popup on the side @done(25-03-25 15:39)
    ✔ used to indicate that songs are rejected due to invalid link or being over the song length limit (red stripe on top) @done(25-03-25 15:39)
    ✔ used to show how many songs from a playlist or album were successfully added to the queue (green stripe on top) @done(25-03-25 15:39)
    ✔ Should have a close button on top right, automatically closes after 5 seconds (maybe indicate the time left as a circle around the close button that decreases as time goes on) @done(25-03-25 15:39)
    ✔ Animation to slide in and out from the right (when it first opens and when it closes) @done(25-03-25 15:39)
☐ implement vote-to-skip, with the threshold being 1 if there is only 1 user in the queue, or a majority vote of the number of users in the queue
    ☐ automatically calls next song if the threshold is met
    ☐ when the user clicks the vote to skip button, it should be light or something to indicate it is active and the number of votes should be displayed
    ☐ the button resets when the song finishes playing/is skipped
    ☐ If the number of users in the queue changes, the vote to skip threshold and the number of users should be updated even if the button is clicked. if the new number of users causes a majority vote to be reached, the song should be skipped.
    ☐ If the user is the only one in the queue, the vote to skip button should just say "skip" and not show the number of votes
✔ Add check for song length limit + reject song from being added if it is above the current limit @done(25-03-25 15:40)
    ✔ Maybe display the current limit on the search page @done(25-03-25 15:40)
✔ On search page, show number of active users @done(25-03-25 00:18)
✔ Show the order in numbers of the user's queue and/or show some way to indicate that you can click + drag the queue to re-order it @done(25-03-24 18:00)
☐ Improve UI and fix buttons for mobile devices
☐ "For mobile UI have the add the queue be a toggle so you can only press it once? Just a suggestion."
☐ I noticed that the light/dark mode switcher isn't always visible when the page loads
3Note: When testing round robin with 1 or 2 other people, also get feedback about UI and navigating the page
If one of the other people is an RTP or eboard member, ask them about the admin functions and if anything should be added or removed